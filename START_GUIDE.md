# 🚀 Пошаговая инструкция: Как запустить проект

## 📋 Что нужно установить (если еще не установлено)

1. **Python 3.12+** - проверка: `python3 --version`
2. **Node.js 20+** - проверка: `node --version`
3. **npm** - проверка: `npm --version`

---

## 🔧 ШАГ 1: Подготовка бэкенда (Python)

### 1.1. Перейдите в папку проекта
```bash
cd /Users/admin/Desktop/Project/dima_miniapp_11_25
```

### 1.2. Создайте виртуальное окружение (если еще не создано)
```bash
cd backend
python3 -m venv .venv
```

### 1.3. Активируйте виртуальное окружение

**На macOS/Linux:**
```bash
source .venv/bin/activate
```

После активации в начале строки терминала появится `(.venv)`.

### 1.4. Установите зависимости Python
```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 1.5. Вернитесь в корень проекта
```bash
cd ..
```

---

## 🎨 ШАГ 2: Подготовка фронтенда (React)

### 2.1. Установите зависимости Node.js
```bash
npm install
```

Это установит все необходимые пакеты из `package.json`.

---

## 🗄️ ШАГ 3: Запуск базы данных (MongoDB и Redis)

### 3.1. Запустите MongoDB через Docker
```bash
npm run docker:mongodb:start
```

### 3.2. Запустите Redis через Docker
```bash
npm run docker:redis:start
```

**Проверка работы:**
```bash
# Проверить статус MongoDB
npm run docker:mongodb:status

# Проверить статус Redis
docker ps | grep redis
```

---

## 🏃 ШАГ 4: Запуск приложения

У вас есть **3 варианта** запуска:

### ✅ ВАРИАНТ 1: Запустить всё одной командой (РЕКОМЕНДУЕТСЯ)

Откройте **один терминал** и выполните:
```bash
npm run dev:full
```

Эта команда запустит и бэкенд, и фронтенд одновременно.

---

### ✅ ВАРИАНТ 2: Запустить отдельно (2 терминала)

**Терминал 1 - Бэкенд:**
```bash
npm run dev:backend
```

**Терминал 2 - Фронтенд:**
```bash
npm run dev:frontend
```

---

### ✅ ВАРИАНТ 3: Запустить вручную (для понимания)

**Терминал 1 - Бэкенд:**
```bash
cd backend
source .venv/bin/activate
uvicorn app.main:app --reload --port 8000
```

**Терминал 2 - Фронтенд:**
```bash
npm run dev
```
или
```bash
vite
```

---

## 🌐 Где открыть приложение

После запуска:

- **Фронтенд:** http://localhost:5173 (или другой порт, если 5173 занят)
- **Бэкенд API:** http://localhost:8000
- **Health Check:** http://localhost:8000/health

---

## ⚙️ Настройка переменных окружения (если нужно)

Создайте файл `.env` в корне проекта (если его нет):

```env
# Бэкенд
MONGODB_URI=mongodb://localhost:27017/miniapp
REDIS_URL=redis://localhost:6379
TELEGRAM_BOT_TOKEN=your_bot_token_here
PUBLIC_URL=http://localhost:8000

# Фронтенд
VITE_API_URL=http://localhost:8000/api
```

---

## 🛑 Как остановить

1. **Остановить приложение:** Нажмите `Ctrl + C` в терминале(ах)
2. **Остановить MongoDB:**
   ```bash
   npm run docker:mongodb:stop
   ```
3. **Остановить Redis:**
   ```bash
   npm run docker:redis:stop
   ```

---

## ❗ Частые проблемы

### Проблема: "Module not found" или "Cannot find module"
**Решение:** Выполните `npm install` в корне проекта

### Проблема: "Python module not found"
**Решение:** 
1. Убедитесь, что виртуальное окружение активировано (видите `(.venv)` в терминале)
2. Выполните `pip install -r backend/requirements.txt`

### Проблема: "Port 8000 already in use"
**Решение:** 
- Либо остановите процесс, занимающий порт
- Либо измените порт в команде запуска: `--port 8001`

### Проблема: "MongoDB connection failed"
**Решение:**
1. Проверьте, что MongoDB запущен: `npm run docker:mongodb:status`
2. Если не запущен: `npm run docker:mongodb:start`

### Проблема: "Cannot connect to backend"
**Решение:**
1. Убедитесь, что бэкенд запущен на порту 8000
2. Проверьте `http://localhost:8000/health` в браузере
3. Убедитесь, что в `.env` указан правильный `VITE_API_URL`

---

## 📝 Полезные команды

```bash
# Просмотр логов MongoDB
npm run docker:mongodb:logs

# Просмотр логов Redis
npm run docker:redis:logs

# Перезапуск MongoDB
npm run docker:mongodb:restart

# Сборка фронтенда для продакшена
npm run build

# Просмотр собранного фронтенда
npm run preview
```

---

## ✅ Чеклист перед запуском

- [ ] Python 3.12+ установлен
- [ ] Node.js 20+ установлен
- [ ] Виртуальное окружение создано и активировано
- [ ] Зависимости Python установлены (`pip install -r backend/requirements.txt`)
- [ ] Зависимости Node.js установлены (`npm install`)
- [ ] MongoDB запущен (`npm run docker:mongodb:start`)
- [ ] Redis запущен (`npm run docker:redis:start`)
- [ ] Файл `.env` создан (если нужен)

---

**Готово! Теперь вы можете запустить проект! 🎉**

