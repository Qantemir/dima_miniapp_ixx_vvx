# 🎯 Явная конфигурация фронтенда для Railway

## Что было сделано

Созданы файлы, которые **явно указывают Railway**, что это фронтенд-проект:

### 1. `railway.toml` - главная конфигурация
```toml
[build]
builder = "nixpacks"

[deploy]
startCommand = "npx serve -s dist -l $PORT"
```
Этот файл **явно говорит Railway**, как запускать фронтенд.

### 2. `nixpacks.toml` - улучшенная конфигурация сборки
- Явно указывает Node.js как единственную зависимость
- Четко описывает этапы сборки фронтенда
- Добавлены логи для отладки

### 3. `Procfile` - явная команда запуска
```
web: npx serve -s dist -l $PORT
```
Railway **обязательно** использует эту команду для запуска.

### 4. `package.json` - добавлен скрипт `start`
```json
"start": "npx serve -s dist -l ${PORT:-3000}"
```
Альтернативный способ запуска, если Railway использует npm scripts.

### 5. `.railwayignore` - исключает бэкенд
Исключает папку `backend/` из контекста фронтенд-сервиса.

---

## ✅ Проверка в Railway

После деплоя в логах фронтенд-сервиса вы должны увидеть:

```
🔨 Building frontend...
📦 Dependencies installed
🏗️  Building React app...
✅ Build completed
🚀 Starting frontend server...
```

**Если видите:**
- ❌ `uvicorn` - Railway всё ещё использует бэкенд-конфигурацию
- ❌ `python` - Railway неправильно определил тип проекта
- ✅ `serve` или `npm start` - **Всё правильно!**

---

## 🔧 Если проблема осталась

1. **Проверьте Root Directory:**
   - Settings → Service → Root Directory
   - Должно быть **ПУСТО** (не `backend`!)

2. **Перезапустите деплой:**
   - Settings → Deployments → Redeploy

3. **Проверьте логи:**
   - Deployments → View Logs
   - Ищите команды `npm ci`, `npm run build`, `serve`

4. **Удалите и создайте заново:**
   - Если ничего не помогает, удалите сервис
   - Создайте новый через "+ Create" → "GitHub Repo"
   - **Root Directory оставьте ПУСТЫМ**

---

## 📝 Структура файлов

```
корень проекта/
├── railway.toml          ← ЯВНАЯ конфигурация для Railway
├── nixpacks.toml         ← Конфигурация сборки фронтенда
├── Procfile              ← Команда запуска фронтенда
├── package.json          ← Со скриптом "start"
├── .railwayignore        ← Исключает backend/
└── backend/              ← НЕ используется фронтендом
    └── nixpacks.toml     ← Для бэкенд-сервиса
```

---

**Теперь Railway точно знает, что запускать фронтенд! 🚀**

