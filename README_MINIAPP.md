# Telegram Shop Mini App

Фронтенд часть Telegram Mini App для интернет-магазина.

## 🚀 Что создано

### Клиентская часть
- ✅ **Каталог товаров** с категориями и вариантами
- ✅ **Корзина** с управлением позициями
- ✅ **Оформление заказа** с формой контактных данных
- ✅ **Просмотр заказа** с возможностью редактирования адреса
- ✅ Редактирование адреса **блокируется** когда статус = `выехал`, `завершён` или `отменён`

### Админская часть
- ✅ **Список заказов** с фильтрацией по статусам
- ✅ **Детальный просмотр заказа** с полной информацией
- ✅ **Изменение статусов** заказов
- ✅ Автоматическое определение админа по `user.id`

### Интеграция с Telegram
- ✅ Полная интеграция с Telegram WebApp API
- ✅ Использование `MainButton` и `BackButton`
- ✅ Поддержка темной/светлой темы Telegram
- ✅ Адаптивный дизайн под мобильные устройства
- ✅ Popup уведомления через Telegram API

## 📋 Технологии

- **React 18** + **TypeScript**
- **Vite** для сборки
- **Tailwind CSS** для стилизации
- **shadcn/ui** компоненты
- **React Router** для навигации
- **Telegram WebApp API**

## 🔧 Установка и запуск

### 1. Установите зависимости
```bash
npm install
```

### 2. Настройте переменные окружения
Создайте файл `.env` на основе `.env.example`:

```env
VITE_API_URL=http://localhost:8000/api
VITE_ADMIN_IDS=123456789,987654321
```

**Важно:** `VITE_ADMIN_IDS` должен содержать те же Telegram ID администраторов, что и в `config.py` вашего Python бота.

### 3. Запустите dev-сервер
```bash
npm run dev
```

Приложение будет доступно на `http://localhost:8080`

### 4. Соберите для продакшена
```bash
npm run build
```

Статические файлы будут в папке `dist/`

## 🔌 Интеграция с Python бэкендом

### Что нужно реализовать на Python стороне

#### 1. API эндпоинты

Создайте API слой (FastAPI или Flask) со следующими эндпоинтами:

**Клиентские:**
- `GET /api/catalog` - каталог товаров
- `GET /api/cart?user_id={id}` - корзина
- `POST /api/cart` - добавить в корзину
- `DELETE /api/cart/item` - удалить из корзины
- `POST /api/order` - создать заказ
- `GET /api/order/last?user_id={id}` - последний активный заказ
- `PATCH /api/order/{id}/address` - изменить адрес (с проверкой статуса!)

**Админские:**
- `GET /api/admin/orders` - список заказов (с фильтром по статусу)
- `GET /api/admin/order/{id}` - детали заказа
- `PATCH /api/admin/order/{id}/status` - изменить статус

#### 2. Логика запрета редактирования адреса

В `PATCH /api/order/{id}/address` обязательно проверяйте:

```python
prohibited_statuses = ['выехал', 'завершён', 'отменён']
if order.status in prohibited_statuses:
    return {"error": "Адрес нельзя изменить", "status_code": 403}
```

#### 3. Уведомления администратору

- При создании заказа (`POST /api/order`) → отправить сообщение админу через бота
- При изменении адреса (`PATCH /api/order/{id}/address`) → отправить сообщение админу

**Не отправлять** клиенту Telegram-сообщения при смене статусов! Клиент видит статус только в мини-аппе.

#### 4. Раздача статики

Настройте Python приложение для раздачи статических файлов:

```python
from fastapi.staticfiles import StaticFiles

app.mount("/", StaticFiles(directory="dist", html=True), name="static")
```

### 5. Интеграция с ботом

В файле бота (`bot.py`) добавьте кнопку для открытия Mini App:

```python
from aiogram.types import WebAppInfo, InlineKeyboardButton, InlineKeyboardMarkup

# Для клиентов
keyboard = InlineKeyboardMarkup(inline_keyboard=[
    [InlineKeyboardButton(
        text="🛍 Открыть магазин",
        web_app=WebAppInfo(url="https://your-domain.com")
    )]
])

# Для админов
admin_keyboard = InlineKeyboardMarkup(inline_keyboard=[
    [InlineKeyboardButton(
        text="📊 Админ-панель",
        web_app=WebAppInfo(url="https://your-domain.com")
    )]
])
```

## 🎨 Дизайн система

Приложение автоматически адаптируется под тему Telegram (светлая/темная) используя:
- Telegram theme parameters
- CSS переменные в `src/index.css`
- Tailwind theme в `tailwind.config.ts`

## 📱 Структура проекта

```
src/
├── components/         # React компоненты
│   ├── ProductCard.tsx
│   ├── CartItem.tsx
│   ├── OrderStatusBadge.tsx
│   └── ui/            # shadcn компоненты
├── pages/             # Страницы приложения
│   ├── CatalogPage.tsx
│   ├── CartPage.tsx
│   ├── CheckoutPage.tsx
│   ├── OrderPage.tsx
│   ├── AdminOrdersPage.tsx
│   └── AdminOrderDetailPage.tsx
├── lib/
│   ├── telegram.ts    # Telegram WebApp утилиты
│   ├── api.ts         # API клиент
│   └── utils.ts
├── types/
│   ├── api.ts         # TypeScript типы для API
│   └── telegram.d.ts  # Telegram WebApp типы
└── App.tsx            # Главный компонент с роутингом
```

## 🔐 Безопасность

1. **Проверка админов** происходит на клиенте по Telegram ID, но **все критичные операции должны проверяться на сервере!**

2. **Валидация статусов** при изменении адреса должна быть на сервере

3. **API должен проверять** `user_id` в каждом запросе

## 🚢 Деплой на Railway

1. Соедините репозиторий с Railway
2. Настройте переменные окружения:
   ```
   VITE_API_URL=https://your-app.railway.app/api
   VITE_ADMIN_IDS=123456789,987654321
   ```
3. Railway автоматически:
   - Установит зависимости (`npm install`)
   - Соберёт фронтенд (`npm run build`)
   - Запустит Python сервер

## ❓ Частые вопросы

**Q: Как протестировать Mini App локально?**

A: Используйте [Telegram Bot API Test Environment](https://core.telegram.org/bots/webapps#testing-mini-apps) или ngrok для туннелирования локального сервера.

**Q: Почему мини-апп не видит данные пользователя?**

A: Убедитесь, что:
1. Подключён скрипт `https://telegram.org/js/telegram-web-app.js`
2. Мини-апп открывается **из бота**, а не напрямую в браузере
3. В консоли нет ошибок инициализации

**Q: Как добавить больше админов?**

A: Добавьте их Telegram ID в `.env` файл через запятую и в `config.py` вашего Python бота.

## 📝 TODO для интеграции

- [ ] Создать API эндпоинты на Python стороне
- [ ] Настроить раздачу статики через Python сервер
- [ ] Добавить кнопки WebApp в бота
- [ ] Настроить переменные окружения с админскими ID
- [ ] Протестировать все сценарии использования
- [ ] Задеплоить на Railway

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12)
2. Проверьте логи Python сервера
3. Убедитесь, что все переменные окружения настроены правильно
4. Проверьте, что API доступен и возвращает правильные данные

---

**Важно:** Этот фронтенд - только UI часть. Вся бизнес-логика, работа с БД и безопасность должны быть реализованы на Python стороне!
