# 🔧 Исправление: Railway ищет Dockerfile вместо nixpacks

## Проблема

Railway выдает ошибку: `couldn't locate a dockerfile at path /Dockerfile`

Это означает, что Railway все еще пытается использовать Dockerfile, хотя должен использовать nixpacks.

## Решение

### Шаг 1: Проверьте настройки в Railway (ВАЖНО!)

1. Откройте ваш фронтенд-сервис в Railway
2. Перейдите в **Settings** → **Build**
3. Найдите раздел **"Build Command"** или **"Builder"**
4. Убедитесь, что выбран **"Nixpacks"** или **"Auto"**
5. **НЕ** должно быть указано **"Dockerfile"** или путь к Dockerfile

### Шаг 2: Убедитесь, что файлы на месте

В корне проекта должны быть:
- ✅ `railway.toml` - с `builder = "nixpacks"`
- ✅ `nixpacks.toml` - конфигурация сборки
- ✅ `package.json` - для определения Node.js проекта
- ❌ **НЕТ** `Dockerfile` в корне (только `Dockerfile.backend`)

### Шаг 3: Перезапустите деплой

1. В Railway нажмите **"Redeploy"**
2. Или сделайте новый коммит и запушьте

## Проверка правильной работы

После исправления в логах должно быть:

```
🔨 Building frontend...
📦 Dependencies installed
🏗️  Building React app...
✅ Build completed
🚀 Starting frontend server...
```

И команды:
- ✅ `npm ci`
- ✅ `npm run build`
- ✅ `npx serve -s dist`

**НЕ должно быть:**
- ❌ `couldn't locate a dockerfile`
- ❌ `docker build`
- ❌ `FROM node:`

## Если проблема осталась

1. **Удалите сервис и создайте заново:**
   - Удалите текущий фронтенд-сервис
   - Создайте новый через "+ Create" → "GitHub Repo"
   - **Root Directory оставьте ПУСТЫМ**
   - Railway автоматически определит nixpacks по `package.json`

2. **Или явно укажите в настройках:**
   - Settings → Build → Builder: выберите **"Nixpacks"**
   - Build Command: оставьте пустым (Railway сам определит)

