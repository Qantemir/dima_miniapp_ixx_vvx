# 🔧 Исправление: Railway ищет Dockerfile.frontend

## Проблема

Railway выдает ошибку: `failed to read Dockerfile at 'Dockerfile.frontend'`

Это означает, что в настройках сервиса в Railway указан путь к Dockerfile.

## Решение

### Шаг 1: Проверьте настройки в Railway

1. Откройте ваш фронтенд-сервис в Railway
2. Перейдите в **Settings** → **Build**
3. Найдите поле **"Dockerfile Path"** или **"Dockerfile"**
4. **ОЧИСТИТЕ** это поле (оставьте пустым)
5. Или убедитесь, что указан **"nixpacks"** в качестве builder

### Шаг 2: Убедитесь, что railway.toml правильный

Файл `railway.toml` должен содержать:
```toml
[build]
builder = "nixpacks"
```

### Шаг 3: Перезапустите деплой

1. В Railway нажмите **"Redeploy"**
2. Или сделайте новый коммит и запушьте

## Альтернативное решение

Если Railway все еще ищет Dockerfile, можно создать пустой файл:

```bash
touch Dockerfile.frontend
```

И добавить в него комментарий:
```
# This file is ignored - Railway should use nixpacks.toml instead
# See railway.toml for build configuration
```

Но лучше **очистить настройки в Railway** и использовать nixpacks.

## Проверка

После исправления в логах должно быть:
- ✅ `🔨 Building frontend...`
- ✅ `npm ci`
- ✅ `npm run build`
- ✅ `serve`

НЕ должно быть:
- ❌ `failed to read Dockerfile`
- ❌ `docker build`

