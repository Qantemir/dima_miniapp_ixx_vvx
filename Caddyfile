# Минимальная конфигурация Caddy для подавления предупреждений
# Railway обычно использует свою конфигурацию, но этот файл может помочь

{
  # Отключаем admin endpoint (убирает предупреждение)
  admin off
  
  # Подавляем предупреждения о HTTP/2/3 (Railway использует свой TLS прокси)
  log {
    level INFO
    output discard
  }
}

# Railway автоматически добавляет свой прокси, поэтому здесь ничего не нужно

