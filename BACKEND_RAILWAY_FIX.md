# 🔧 Исправление: Бэкенд пытается запустить фронтенд-команду

## Проблема

Бэкенд-сервис в Railway пытается запустить `serve` (команда фронтенда) вместо `uvicorn` (команда бэкенда).

## Причина

Бэкенд-сервис использует корневой `Procfile` или `nixpacks.toml` вместо файлов из папки `backend/`.

## Решение

### В Railway настройках бэкенд-сервиса:

1. Откройте бэкенд-сервис в Railway
2. Перейдите в **Settings** → **Service**
3. Найдите поле **"Root Directory"**
4. Убедитесь, что указано: **`backend`** (не пусто!)
5. Сохраните изменения
6. Нажмите **"Redeploy"**

### Проверка:

После исправления бэкенд должен использовать:
- ✅ `backend/nixpacks.toml` - конфигурация сборки Python
- ✅ `backend/Procfile` - команда `uvicorn app.main:app`
- ✅ `backend/railway.toml` - явная конфигурация (создан)

**НЕ должен использовать:**
- ❌ Корневой `Procfile` (с командой `serve`)
- ❌ Корневой `nixpacks.toml` (для Node.js)

## Структура файлов

```
корень проекта/
├── Procfile              ← Для фронтенда (serve)
├── nixpacks.toml         ← Для фронтенда (Node.js)
├── railway.toml          ← Для фронтенда
└── backend/
    ├── Procfile          ← Для бэкенда (uvicorn) ✅
    ├── nixpacks.toml     ← Для бэкенда (Python) ✅
    └── railway.toml      ← Для бэкенда (создан) ✅
```

## После исправления

В логах бэкенда должно быть:
- ✅ `python3.12 -m venv .venv`
- ✅ `pip install -r requirements.txt`
- ✅ `.venv/bin/uvicorn app.main:app`

**НЕ должно быть:**
- ❌ `serve: command not found`
- ❌ `npm ci`
- ❌ `npm run build`

